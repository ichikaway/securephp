For CakePHP users.

Set the SecurePHP library in your plugins directory.

Import secure_php bootstrap in webapp/index.php, require_once must be called before Dispatcher as follow.
<code>
  } else {
    //import secure_php bootstrap
    require_once('YOUR_PLUGIN_PATH/plugins/securephp/Config/bootstrap.php');

    $Dispatcher = new Dispatcher();
    $Dispatcher->dispatch();
  }
</code>


